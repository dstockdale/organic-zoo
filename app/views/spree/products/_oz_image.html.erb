<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:product), { class: "img-responsive", itemprop: "image"} %>
<% else %>
  <%= product_image(@product, { class: "img-responsive", itemprop: "image" }) %>
<% end %>